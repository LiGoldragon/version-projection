# version-projection

`version-projection` owns workspace-universal primitives for projecting
typed records between adjacent component versions.

It is library-only: no daemon, no socket, no durable store, and no
Persona-specific component payloads.
